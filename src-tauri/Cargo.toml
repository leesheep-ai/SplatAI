[package]
name = "splatai-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "splatai_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
